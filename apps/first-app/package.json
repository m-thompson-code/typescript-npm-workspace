{
    "name": "@example/first-app",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "dev": "npm run build -w ../../libs/shared && ts-node src/index",
        "build": "vite build",
        "prod": "npm run build && node dist/index.cjs.js"
    }
}
